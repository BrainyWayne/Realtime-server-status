<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.realtimeserverstatus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".MVASnapshot.MvaDaily"></activity>
        <activity android:name=".MVASnapshot.MvaWeekly" />
        <activity android:name=".MVASnapshot.MvaMonthly" />
        <activity android:name=".MVASnapshot.MVARegistrations" />
        <activity android:name=".MVASnapshot.MVAHomePage" />
        <activity android:name=".ServerStatus.PieChartActivity" />
        <activity android:name=".ServerStatus.ServerDetails" />
        <activity android:name=".ServerStatus.HomePage" />
        <activity android:name=".LoginSuccess" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>